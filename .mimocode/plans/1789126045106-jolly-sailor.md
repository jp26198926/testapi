# Plan: Add Limit Selector to Public API Pages

## Goal
Add a configurable page size selector and "Showing X–Y of Z" label to both public API pages, matching the pagination UI from the collection detail page.

## Files to Modify
1. `app/(dashboard)/my/public-api/page.tsx` (dashboard version)
2. `app/public-api/page.tsx` (standalone version)

## Changes (same for both files)

### 1. Add state: `limit` and `total`
- `limit: number` — default 10 (current hardcoded value)
- `total: number` — total record count from API response

### 2. Update fetch to use `limit` state and extract `total`
Change `fetch(...limit=10)` to `fetch(...limit=${limit})` and add `setTotal(d.pagination?.total || 0)`.

### 3. Add `limit` to useEffect dependency array

### 4. Enhance pagination UI
Replace the simple Prev/Next with:
- "Showing X–Y of Z records" label
- Per-page dropdown (10 / 20 / 50 / 100) — resets page to 1 on change
- Prev / Next buttons (keep existing)
- "Page 1 of N" indicator (keep existing)

## Verification
- Visit both `/public-api` and `/my/public-api`
- Change per-page dropdown — verify records reload with new limit
- Navigate pages — verify correct range shown
- Dark mode styling works
