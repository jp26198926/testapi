# Plan: Remove "File Upload" from /public-docs + Dynamic app name

## Changes

### 1. Remove "File Upload" section from `/public-docs`

**File:** `app/public-docs/page.tsx`

Remove the `{ id: "file-upload", ... }` entry from the `SECTIONS` array (lines 82-89). The TOC sidebar and content both derive from `SECTIONS`, so both update automatically.

### 2. Dynamic app name in `/public-docs` (Server Component)

**File:** `app/public-docs/page.tsx`

- Import `siteSettings` from `@/lib/db/schema`
- Query `siteSettings` table at the top: `const settingsRows = await db.select().from(siteSettings).limit(1); const appName = settingsRows[0]?.appName ?? "TESTAPI";`
- Replace 3 hardcoded "TESTAPI" occurrences with `appName`:
  - Line 158: header logo
  - Line 168: description text ("Complete reference for the TESTAPI REST API")
  - Line 281: footer copyright

### 3. Dynamic app name in `/public-api` (Client Component)

**File:** `app/public-api/page.tsx`

This is a `"use client"` page — cannot query DB directly. Use the same pattern as the dashboard layout (`app/(dashboard)/layout.tsx` line 36):
- Add `const [appName, setAppName] = useState("TESTAPI");`
- Add useEffect to fetch from `/api/settings`: `.then((d) => { if (d.data?.appName) setAppName(d.data.appName); })`
- Replace 2 hardcoded "TESTAPI" occurrences with `appName`:
  - Line 77: header logo
  - Line 332: footer copyright

## Files to Modify

| File | Action |
|------|--------|
| `app/public-docs/page.tsx` | **Edit** — remove File Upload section, replace 3 hardcoded "TESTAPI" with DB query |
| `app/public-api/page.tsx` | **Edit** — fetch appName from /api/settings, replace 2 hardcoded "TESTAPI" |

## Verification

1. `/public-docs` — no "File Upload" in sidebar or content
2. `/public-docs` — header, description, footer show dynamic app name from `siteSettings`
3. `/public-api` — header and footer show dynamic app name from `/api/settings`
4. Admin changes `appName` in settings → both pages reflect the new name
