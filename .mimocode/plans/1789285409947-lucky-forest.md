# Plan: Trim seed data to 25 records per collection

## Change

Edit `scripts/seed.ts` to reduce each collection's `data` array to exactly 25 records (currently: Posts=30, Users=30, Comments=38, Products=30, Todos=30).

For each collection, trim the last N entries to keep 25. No structural changes to the seeding logic.

## File

`scripts/seed.ts` — trim `data` arrays
